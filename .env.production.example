# Django Production Settings
SECRET_KEY=replace_with_secure_random_key
DEBUG=False
ALLOWED_HOSTS=politico.example.com,www.politico.example.com

# Database Configuration
DB_NAME=politico_db
DB_USER=politico_user
DB_PASSWORD=change_this_to_secure_password
DB_HOST=db
DB_PORT=5432

# Email Configuration
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True

# CORS Settings
CORS_ORIGIN_WHITELIST=https://politico.example.com,https://www.politico.example.com

# Celery Configuration
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# API Keys
ALTHINGI_API_KEY=your-althingi-api-key

# Note: This is an example configuration file for production.
# Copy this file to .env.production and replace the values with your actual configuration.
# Never commit the actual .env.production file to version control. 